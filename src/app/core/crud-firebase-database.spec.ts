import { CrudFirebaseDatabase } from './crud-firebase-database';

describe('CrudFirebaseDatabase', () => {
  it('should create an instance', () => {
    expect(new CrudFirebaseDatabase()).toBeTruthy();
  });
});
